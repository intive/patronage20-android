package com.intive.patronage.smarthome.navigator

import android.os.Bundle
import com.intive.patronage.smarthome.*

class CoordinatorDashBoard(private val navigator: Navigator, private val bundle: Bundle?) {

    fun goToLedDetailScreen() {
        //uncomment and replace Fragment::class
        //val fragmentEvent = FragmentEvent(LedFragment::class.java, bundle)
        //navigator.goToScreen(fragmentEvent)
    }

    fun goToTemperatureDetailScreen() {
        //uncomment and replace Fragment::class
        //val fragmentEvent = FragmentEvent(TemperatureFragment::class.java, bundle)
        //navigator.goToScreen(fragmentEvent)
    }

    fun goToWindowDetailScreen() {
        //uncomment and replace Fragment::class
        //val fragmentEvent = FragmentEvent(WindowFragment::class.java, bundle)
        //navigator.goToScreen(fragmentEvent)
    }

    fun goToWindowBlindsDetailScreen() {
        //uncomment and replace Fragment::class
        //val fragmentEvent = FragmentEvent(WindowBlindsFragment::class.java, bundle)
        //navigator.goToScreen(fragmentEvent)
    }

    fun goToRFIDDetailScreen() {
        //uncomment and replace Fragment::class
        //val fragmentEvent = FragmentEvent(RFIDFragment::class.java, bundle)
        //navigator.goToScreen(fragmentEvent)
    }

    fun goToSmokeDetailScreen() {
        //uncomment and replace Fragment::class
        //val fragmentEvent = FragmentEvent(SmokeFragment::class.java, bundle)
        //navigator.goToScreen(fragmentEvent)
    }

    fun goToHVACDetailScreen() {
        //uncomment and replace Fragment::class
        //val fragmentEvent = FragmentEvent(HVACFragment::class.java, bundle)
        //navigator.goToScreen(fragmentEvent)
    }

    fun goToHVACRoomsDetailScreen() {
        //uncomment and replace Fragment::class
        //val fragmentEvent = FragmentEvent(HVACRoomsFragment::class.java, bundle)
        //navigator.goToScreen(fragmentEvent)
    }

    fun goToBackDashBoard() {
        //uncomment and replace Fragment::class
        //val fragmentEvent = FragmentEvent(DashBoardFragment::class.java, bundle)
        //navigator.goToScreen(fragmentEvent)
    }
}